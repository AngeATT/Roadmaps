start : 80
*Comment déclarer une variable ?
aucun mot clé

-L'indentation compte en python, tab c'est 4 espaces
-tableaux peuvent tout contenir

"_" est le caractère par defaut, utiliser pour les boucles for par exemple

for _ in range(5) :
-on utilise rarement les {} en python
-dir permet d'avoir toutes les methodes qu'on peut utiliser sur une variable
-help(dir.nom_methode) permet d'avoir la doc sur cette méthode

-Map = dictionnaire en python
Set, list, tuples

114 j1

-random.choice(collections (pas set))

-comment définir une fonction ? 
def nom function : 
	corps fonction

*Comment obtenir le type ?
*Comment obtenir la taille de la collection ?
*C'est quoi une BIF ?
*Comment obtenir toutes les propriétés d'un objet ?
*Comment obtenir la documentation sur une propriété ?
*Comment choisir au hasard dans une list ?
*Peut on choisir au hasard dans un set ? Pourquoi ?
*Comment vérifier qu'une variable est dans une collection ?
*Comment appelle t-on les fonctions disponible à l'installation de python ?
*Ou trouver des packages, des buil ins fonctions de la communauté ?
*C'est quoi un tuple ?


==Nouveau projet pour lire des données d'enregistrement depuis un fichier
*Comment mettre string en maj ? en minuscule ?
*Différence entre BIFS et les .fn() ?
*Quelles sont les objets mutables ?
*Comment diviser un str en fonction d'un pattern ?
*Comment supprimer le suffixe ?
*C'est quoi le unpacking ?
216

*C'est quoi le with en python ?
permet de fermer la ressource après l'avoir utiliser, par exemple pour un fichier c'est une fermeture du fichier

